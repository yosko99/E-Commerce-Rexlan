const stripQueryFromUrl = require('./stripQueryFromUrl');
const uniqueSwatches = require('./uniqueSwatches');
const formSubmitUrl = require('./formSubmitUrl');
const uniqueSizes = require('./uniqueSizes');

module.exports = {
  stripQueryFromUrl: stripQueryFromUrl,
  uniqueSwatches: uniqueSwatches,
  formSubmitUrl: formSubmitUrl,
  uniqueSizes: uniqueSizes
};
